import { apiURL } from '../settings.js';
import { applicationState } from '../main.js';

const usersInterface = document.getElementById('app-users');

async function initUsers() {
    console.log('got online users');
}

export { initUsers };
